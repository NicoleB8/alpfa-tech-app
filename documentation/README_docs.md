# Introduction to ALPFA: Documentation

This is the start of the documentation webpage

To generate documentation, inside the `cmd.exe` or your preferred terminal, run the following code:

```
npm run docs
```

It will run the script inside the `package.json` file that will generate the documentation and open it.

